package repository;

import model.entities.Disciplina_Matriz;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Disciplina_MatrizRepository extends JpaRepository<Disciplina_Matriz, Integer> {
}
